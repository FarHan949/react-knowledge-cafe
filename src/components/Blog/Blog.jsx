import PropTypes from 'prop-types'

const Blog = ({blog}) => {
    return (
        <div>
            
        </div>
    );
};

Blog.PropTypes = {
    Blog : PropTypes.object.isRequired
}
export default Blog;